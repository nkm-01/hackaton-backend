# Админ-панель: Управление документами

Руководство по использованию админ-панели Django для управления документами.

## Доступ к админке

URL: `http://localhost:8000/admin/`

После авторизации перейдите в раздел **Documents → Документы**

## Список документов

### Отображаемые колонки:
- **Название** - название документа
- **Тип файла** - PDF, RTF, DOCX, TXT
- **Статус** - цветная индикация:
  - 🟠 Оранжевый - Ожидает обработки (pending)
  - 🔵 Синий - Обрабатывается (processing)
  - 🟢 Зеленый - Обработан (processed)
  - 🔴 Красный - Ошибка (error)
- **Размер** - размер файла в человекочитаемом формате
- **Количество страниц** - оценочное количество
- **Дата загрузки**

### Фильтры:
- По статусу обработки
- По типу файла
- По дате загрузки

### Поиск:
Поиск по названию документа

## Загрузка нового документа

1. Нажмите **"Добавить документ"** в правом верхнем углу
2. Заполните поля:
   - **Название** - введите название документа
   - **Файл** - выберите файл (PDF, RTF, DOCX, TXT)
   - **Тип файла** - выберите из списка
3. Нажмите **"Сохранить"**

**Автоматически:**
- Файл сохранится в медиа-хранилище
- Начнется обработка документа в фоне
- Появится уведомление: "Документ загружен и отправлен на обработку"

## Просмотр и редактирование документа

Кликните на название документа в списке для открытия детальной страницы.

### Разделы:

#### 1. Основная информация
- ID документа (UUID, неизменяемый)
- Название
- Файл (ссылка на скачивание)
- Тип файла

#### 2. Метаданные
- Размер файла (неизменяемый)
- Количество страниц (неизменяемое)
- Дата загрузки (неизменяемая)

#### 3. Статус обработки
- **Статус** - текущий статус
- **Сообщение об ошибке** - если статус = error
- **Действия** - кнопки управления

### Кнопки действий:

#### 🔄 Пересканировать
- Доступна для всех статусов кроме "Обрабатывается"
- **Что делает:**
  - Сбрасывает статус на "pending"
  - Очищает сообщение об ошибке
  - Запускает новую обработку документа
- **Когда использовать:**
  - При ошибке обработки
  - Если нужно заново обработать документ
  - После обновления промптов AI

#### ♻️ Переиндексировать
- Доступна только для статуса "Обработан"
- **Что делает:**
  - Удаляет старые данные из Qdrant
  - Запускает повторную обработку и индексацию
- **Когда использовать:**
  - После изменения настроек индексации
  - Для обновления векторного представления

## Массовые действия

Выберите несколько документов (чекбоксы слева) и выберите действие из выпадающего списка:

### 1. Пересканировать выбранные документы
- Запускает обработку для всех выбранных документов
- Пропускает документы со статусом "Обрабатывается"
- Показывает количество запущенных и пропущенных

**Использование:**
- Обработка новых документов
- Повторная обработка после ошибок

### 2. Переиндексировать выбранные документы
- Удаляет старые данные из Qdrant
- Запускает повторную индексацию
- Пропускает документы со статусом "Обрабатывается"

**Использование:**
- Обновление индексов после изменений
- Массовая переиндексация

### 3. Повторить обработку документов с ошибками
- **Умное действие:** автоматически фильтрует только документы с ошибками
- Сбрасывает статус и очищает ошибки
- Запускает обработку

**Использование:**
- Быстрое исправление ошибок после решения проблемы
- Пакетная обработка неудачных документов

## Удаление документов

### Удаление одного документа:
1. Откройте документ
2. Нажмите **"Удалить"** внизу страницы
3. Подтвердите удаление

### Массовое удаление:
1. Выберите документы (чекбоксы)
2. Выберите действие **"Удалить выбранные документы"**
3. Подтвердите удаление

**При удалении автоматически:**
- Удаляется файл из файловой системы
- Удаляются все точки документа из Qdrant
- Удаляется запись из БД

## Работа с ошибками

### Если документ имеет статус "Ошибка":

1. **Просмотрите ошибку:**
   - Откройте документ
   - Прочитайте поле "Сообщение об ошибке"

2. **Частые ошибки:**
   - Поврежденный файл → загрузите файл заново
   - Ошибка извлечения текста → проверьте формат файла
   - Ошибка Qdrant → проверьте подключение к Qdrant
   - Ошибка LLM API → проверьте API ключ

3. **Исправление:**
   - Нажмите кнопку **"🔄 Пересканировать"**
   - Или используйте массовое действие **"Повторить обработку документов с ошибками"**

## Мониторинг обработки

### Проверка статуса:
1. Обновите страницу (F5) для проверки статуса
2. Статус "Обрабатывается" → документ в процессе
3. Статус "Обработан" → готов к использованию

### Время обработки:
- Маленькие документы (< 10 страниц): ~30-60 секунд
- Средние документы (10-50 страниц): ~1-3 минуты
- Большие документы (> 50 страниц): ~3-10 минут

### Что происходит при обработке:
1. Извлечение текста из файла
2. Анализ структуры документа (LLM)
3. Разделение на секции
4. Генерация эмбеддингов
5. Индексация в Qdrant

## Советы по использованию

### ✅ Лучшие практики:

1. **Называйте документы понятно**
   - Плохо: "doc1.pdf"
   - Хорошо: "Приказ по охране труда № 123 от 2023"

2. **Проверяйте статус после загрузки**
   - Дождитесь статуса "Обработан"
   - При ошибке - сразу пересканируйте

3. **Используйте массовые действия**
   - Загружайте несколько документов
   - Используйте массовую обработку ошибок

4. **Регулярно проверяйте ошибки**
   - Фильтр по статусу = "Ошибка"
   - Используйте "Повторить обработку документов с ошибками"

### ⚠️ Ограничения:

- Максимальный размер файла: 100 МБ
- Поддерживаемые форматы: PDF, RTF, DOCX, TXT
- Одновременная обработка: неограниченно (фоновые потоки)

### 🔧 Устранение проблем:

**Документ долго обрабатывается:**
- Большие документы требуют времени
- Проверьте логи сервера
- Перезапустите обработку

**Ошибка при загрузке:**
- Проверьте формат файла
- Проверьте размер файла
- Убедитесь, что файл не поврежден

**Документ не появляется в поиске:**
- Дождитесь статуса "Обработан"
- Проверьте, что индексация прошла успешно
- При необходимости переиндексируйте

## Горячие клавиши (стандартные Django Admin)

- `Alt + S` - Сохранить
- `Alt + A` - Сохранить и добавить новый
- `Alt + C` - Сохранить и продолжить редактирование

## Логирование

Все действия логируются в консоль сервера:
- Загрузка документа
- Начало обработки
- Завершение обработки
- Ошибки обработки
- Удаление документа
- Переиндексация

Проверяйте логи в терминале где запущен сервер Django.
